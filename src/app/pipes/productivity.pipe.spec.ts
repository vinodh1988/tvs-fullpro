import { ProductivityPipe } from './productivity.pipe';

describe('ProductivityPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductivityPipe();
    expect(pipe).toBeTruthy();
  });
});
